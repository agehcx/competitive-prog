#include <bits/stdc++.h>
using namespace std;

void solve(int testCase) {
    int n, m; cin >> n >> m; // n, pick m
    int arr[n];
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int Q; cin >> Q;
    while(Q--)
        solve(Q);
}