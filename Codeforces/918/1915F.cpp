#include <bits/stdc++.h>
using namespace std;

void solve(int testCase) {
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int Q; cin >> Q;
    while(Q--) solve(Q);
}